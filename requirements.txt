# Automation dependencies used for build and validation tasks.
pyinstaller>=6.11,<7
pyyaml>=6.0.3,<7

# Runtime dependencies for the SIP/VoIP softphone feature (see
# docs/dependencies.md for the rationale behind each one).
pyVoIP>=1.6.8,<2
sounddevice>=0.5,<1
keyring>=25,<26
