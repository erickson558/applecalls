interface:
  display_name: "AppleCalls Release"
  short_description: "Version, build, and safe GitHub publish"
  default_prompt: "Use $applecalls-release to verify the active GitHub account is erickson558, align Vx.x.x, rebuild AppleCalls, and prepare the matching commit, tag, and push workflow."
